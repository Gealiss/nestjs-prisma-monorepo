# Simple Vite Web App

A minimalist, premium "Hello World" landing page built with Vite and Vanilla CSS.

Run the development server:
   ```bash
   pnpm dev
   ```

Open your browser and navigate to the local URL (usually `http://localhost:5173`).
